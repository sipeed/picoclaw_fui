<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络访问 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- 友盟统计需要 - 获取设备信息 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 保留 dataSync 兼容 flutter_background_service 插件 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Wake Lock 防止 CPU 休眠 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 开机自启 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 通知权限 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="PicoClaw"
        android:name=".PicoClawApp"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- 友盟实时测试需要的 URL Scheme 配置 -->
            <!-- 格式1：um.你的AppKey (友盟实时日志使用，带点号) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="um.${PICOCLAW_UMENG_APP_KEY}" />
            </intent-filter>
            
            <!-- 友盟实时测试需要的 URL Scheme 配置 -->
            <!-- 格式2：um你的AppKey (不带点号，兼容不同版本) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="um${PICOCLAW_UMENG_APP_KEY}" />
            </intent-filter>
        </activity>

        <!-- PicoClaw 原生前台服务（运行 Go 二进制） -->
        <service
            android:name=".service.PicoClawService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Flutter Background Service 插件 -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:replace="android:exported" />

        <!-- 开机自启广播接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        
        <!-- 友盟统计配置 - 从 build.gradle 的 buildConfigField 读取 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="${PICOCLAW_UMENG_APP_KEY}" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="${PICOCLAW_UMENG_CHANNEL}" />
        
        <!-- Firebase Analytics Configuration - 必须配置才能启用 Analytics -->
        <!-- 从 Firebase Console 的 google-services.json 获取这些值 -->
        <meta-data
            android:name="com.google.android.gms.google_app_id"
            android:value="@string/google_app_id" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/google_app_id" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>